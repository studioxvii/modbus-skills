interface:
  display_name: "Review Evidence"
  short_description: "Turn map evidence into decisions"
  default_prompt: "Use $review-evidence to review these source findings and holds."
policy:
  allow_implicit_invocation: false
