interface:
  display_name: "Build Modpoll (BETA)"
  short_description: "Build BETA Modpoll artifacts"
  default_prompt: "Use $build-modpoll to build artifacts for this Modpoll profile."
policy:
  allow_implicit_invocation: false
