interface:
  display_name: "Plan Reads"
  short_description: "Compile bounded Modbus read blocks"
  default_prompt: "Use $plan-reads to compile safe reads for this reviewed map."
policy:
  allow_implicit_invocation: false
