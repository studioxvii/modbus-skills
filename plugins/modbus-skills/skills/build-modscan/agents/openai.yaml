interface:
  display_name: "Build ModScan (BETA)"
  short_description: "Build a BETA ModScan read plan"
  default_prompt: "Use $build-modscan to build a read-only ModScan setup."
policy:
  allow_implicit_invocation: false
