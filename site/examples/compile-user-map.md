# A messy table becomes a user map

This is a fictional commissioning table.

The finished table remaps 40001/30001 notation to protocol offsets and records the write-only setpoint as an exclusion. The same table with byte order left blank pauses so you can choose a layout.

Source files live in `docs/examples/compile-user-map`.
