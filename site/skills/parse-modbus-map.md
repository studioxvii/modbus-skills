# Parse Modbus Map

Parse Modbus register maps from CSV, JSON, XML, XLSX, or structured text into candidate-map artifacts with source values, rejected rows, warnings, assumptions, and holds. Use when a user supplies a register list or wants a machine-readable map without silent engineering guesses.

## Common requests

- Parse this CSV Modbus register map with rejected-row evidence.
- Please parse this CSV Modbus register map with rejected-row evidence.
- Can you parse this CSV Modbus register map with rejected-row evidence.
- I need you to parse this CSV Modbus register map with rejected-row evidence.
- Help me parse this CSV Modbus register map with rejected-row evidence.

## Try it

```text
Use $parse-modbus-map to parse this register map into a reviewed artifact.
```

Source: `plugins/modbus-skills/skills/parse-modbus-map/SKILL.md`
