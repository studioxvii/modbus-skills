[project]
name = "modbus-skills-runtime"
version = "0.1.0"
description = "Deterministic runtime for read-only Modbus skills"
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = ["pdfplumber>=0.11,<0.12"]

[tool.modbus-skills]
plugin-root = "plugins/modbus-skills"
